

function SectionTitle(props) {
    return (
        <>
            <h2 className="{section-title animation-translate-overline animation-item-1}">{props.title}</h2>
        </>
    );
}

export default SectionTitle;
